import type { FastifyPluginAsync } from 'fastify';
import { requireAuth, requirePermission } from '../middleware/auth.js';
import { parsePaginationQuery } from '../pagination.js';
import { requestParam } from '../request-input.js';
import type { SalesReturnRecord } from '../../../domain/contracts/repositories.js';
function ctx(r:any){return{tenantId:r.tenantId,organizationId:r.user.organizationId,branchId:r.user.branchId??r.user.defaultBranchId,financialYearId:r.user.financialYearId,userId:r.user.id};}function out(x:SalesReturnRecord){const{tenantId:_t,...v}=x;return v;}
const routes:FastifyPluginAsync=async(f)=>{f.post('/sales/returns',{preHandler:[requireAuth,requirePermission('sales.return.create')]},async(r:any,h)=>{const x=await f.salesReturnService.create(ctx(r),r.body);h.code(201);return{success:true,return:out(x)};});f.get('/sales/returns',{preHandler:[requireAuth,requirePermission('sales.return.read')]},async(r:any)=>{const p=parsePaginationQuery(r.query);const x=await f.salesReturnService.list(ctx(r),p);return{success:true,returns:x.items.map(out),metadata:{page:p.page,page_size:p.pageSize,total:x.total,total_pages:Math.ceil(x.total/p.pageSize),order:p.order}};});f.get('/sales/returns/:id',{preHandler:[requireAuth,requirePermission('sales.return.read')]},async(r:any)=>({success:true,return:out(await f.salesReturnService.get(ctx(r),requestParam(r.params,'id')??''))}));f.patch('/sales/returns/:id',{preHandler:[requireAuth,requirePermission('sales.return.update')]},async(r:any)=>({success:true,return:out(await f.salesReturnService.update(ctx(r),requestParam(r.params,'id')??'',r.body))}));for(const s of ['inspect','approve','reject','process','close','cancel'] as const)f.post(`/sales/returns/:id/${s}`,{preHandler:[requireAuth,requirePermission(`sales.return.${s}`)]},async(r:any)=>({success:true,return:out(await f.salesReturnService.transition(ctx(r),requestParam(r.params,'id')??'',s==='inspect'?'INSPECTED':s==='approve'?'APPROVED':s==='reject'?'REJECTED':s==='process'?'PROCESSED':s==='close'?'CLOSED':'CANCELLED',r.body.expectedVersion))}));};export default routes;
