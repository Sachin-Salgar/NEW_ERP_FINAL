import type{FastifyPluginAsync}from'fastify';import{requireAuth,requirePermission}from'../middleware/auth.js';const c=(r:any)=>({tenantId:r.tenantId,organizationId:r.user.organizationId,userId:r.user.id});const x:FastifyPluginAsync=async f=>{f.post('/sales/discount-rules',{preHandler:[requireAuth,requirePermission('sales.discount.create')]},async(r:any,h)=>{h.code(201);return{success:true,discountRule:await f.discountService.create(c(r),r.body)}});f.get('/sales/discount-rules',{preHandler:[requireAuth,requirePermission('sales.discount.read')]},async(r:any)=>({success:true,discountRules:await f.discountService.list(c(r))}));for(const s of['publish','archive']as const)f.post(`/sales/discount-rules/:id/${s}`,{preHandler:[requireAuth,requirePermission(`sales.discount.${s}`)]},async(r:any)=>({success:true,discountRule:await f.discountService.transition(c(r),r.params.id,s==='publish'?'PUBLISHED':'ARCHIVED',r.body.expectedVersion)}));};export default x;
